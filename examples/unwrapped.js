const _format = imports._format; 

log(Object.getOwnPropertyNames(_format));

const gettext = imports.gettext; 

log(Object.getOwnPropertyNames(gettext));

const _signals = imports._signals; 

log(Object.getOwnPropertyNames(_signals));

const $ = imports.iconsole;

log(Object.getOwnPropertyNames($));

const system = imports.system; 

log(Object.getOwnPropertyNames(system));